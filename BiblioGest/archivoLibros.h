#pragma once
#include <string>
#include "Libro.h"
/*
class ArchivoLibros
{
private:
    std::string _nombreArchivo;
public:
    ArchivoLibros (std::string _nombreArchivo);
    bool agregarRegistroLibro(Libro objlibro);
    Libro leerRegistroLibro(int pos);
    int buscarRegistroLibroPorISBN(const char* _ISBN);
    int cuentaLibrosRegistrados();
    //MUESTRA TODOS LOS LIBROS
//    void mostrarBiblioteca();

*/
